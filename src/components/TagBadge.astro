---
interface Props {
  tag: string;
}
const { tag } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="px-2 py-1 text-xs font-medium bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 rounded-full hover:bg-blue-200 dark:hover:bg-blue-800 transition-colors"
>
  #{tag}
</a>
